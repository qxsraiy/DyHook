## DyHook v1.3.0 — 自动发件开关 + 待发件列表页

### 1. 新增「自动发件」开关

**开启时**：解析完直接发到论坛，**只用提示条（Toast）告知成功/失败**，不弹常驻通知。

```
✅ 已发件：标题…
❌ 发件失败：原因…（文件已保留，可在模块里手动发）
```

**关闭时**：保持原来的手动流程 —— 通知栏「解析完成 + 发件按钮」→ 点击后发件。

配置键：`auto_send`（在模块设置页第一项卡片里）

### 2. 新增「待发件」页面

底栏两个图标切换：

```
⚙ 设置          📄 待发件
```

**待发件页**列出还没上传的文件：

- **AI 开启** → 只显示处理后的成品（`青空_*.txt`）
- **AI 关闭** → 显示源文件（`源_*.txt`）
- **标题完整展示，不折叠**（最多 20 行，可换行）
- 每行右侧有 **「发件」** 按钮
- 行下方显示 修改时间 · 文件大小 · 文件名

> 判断「未上传」的逻辑很直接：**发件成功会删掉文件**，
> 所以**列表里存在的 = 还没发出去的**。发完自动从列表消失。

### 3. 发件逻辑重构

- 抽出统一的 `doSend()`，自动发件（Toast 模式）和手动发件（通知模式）共用
- 自动发件同样享受**最多 10 次 / 每次隔 3 秒**的重试
- 成功后删除**源文件 + 成品文件**
- 界面发件会自动找到配对的源文件一起删

### 4. 之前版本已包含

- 作者名**五级兜底**获取（`ArticleDetailInfo` → `ArticleDetailResponse` →
  `articleId` 严格匹配 → 文章类 Aweme → `feData`/`articleExtra` JSON）
- 正文格式：`@作者by抖音` + 空 1 行 + 正文
- AI 清洗：去 HTML 标签/实体、`<br>` 转换行、修正重复引号、去分享口令、轻度书面化
- 通知栏状态流转：解析完成 → 发件中 → 发件成功/失败（按钮自动消失）
- 发件幂等锁 + 路径白名单守卫
- 修复 APK 安装失败（`resources.arsc` 必须不压缩 + 4 字节对齐）

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限

> ⚠ 卸载重装会重置 `MANAGE_EXTERNAL_STORAGE` 授权，重装后请在模块界面确认存储权限，
> 否则会读不到 `config.txt`（表现为「AI 未开启」）。

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
