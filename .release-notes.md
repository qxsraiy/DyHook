## DyHook v1.4.3 — 本地洗稿能力增强

基于 **v1.4.2**，**只改动了本地正则清洗**（`AiClient.cleanText()`），其它功能一行未动。

### 新增的清洗规则（纯本地，不联网、不依赖 AI）

**HTML**
- `<br>` / `<br/>` / `<br />` → 真换行
- `<p>` `</p>` `<div>` `<li>` `<tr>` `<h1-6>` `<blockquote>` 等块级标签 → 换行
- `span/strong/b/em/a/img/table/ul/pre/code/iframe/video` 等 → 删除
- 实体：`&nbsp;` `&amp;` `&lt;` `&quot;` `&mdash;` `&hellip;` `&ldquo;` `&rdquo;` 等 + 数字实体 `&#96;`

**代码框（Flarum 会渲染成可复制代码块的元凶）**
- ` ``` ` / `~~~` 围栏整行删
- 行内反引号删
- 行首 4+ 空格 / Tab / 全角空格 / 不换行空格 → 删

**分享口令与引流话术（整行删，12 条模式）**
- `2H-:/a :5pm U@L.wS 07/07 …^^xDTreMGbCaY99`
- `长按复制打开抖音，即可阅读文章`
- `复制打开抖音` / `复制链接打开抖音`
- `看看【某某的作品】` / `【某某的作品】`
- `打开抖音看更多/搜索`
- `抖音搜索` / `抖音扫一扫`
- `关注我/点赞收藏不迷路`
- `点击下方链接查看`
- `转发/分享自微博/微信/头条/小红书`

**排版噪声**
- 零宽字符（`\u200B-\u200F`）、BOM、控制字符
- 占位符 `[图片]` `[视频]` `[表情]` `[音乐]` `[话题]` `[商品]` 等 15 种
- 重复标点收敛：`。。。。。` → `。`，`！！！！` → `！`
- 省略号规范化：`...` `。。。` `···` → `……`
- 引号规范化：`""文字""` → `“文字”`
- 中文之间的多余空格
- 行尾空白、连续空行收敛

### 实测（论坛真实渲染结果）

```
输入: 第一段文字<br/>第二段开始。<br/><br/>    这行有四个空格缩进。
      2H-:/a :5pm U@L.wS 07/07 【测试】长按复制打开抖音，即可阅读文章 ^^xDTreMGbCaY99
      复制打开抖音，看看【某某的作品】
      还有&nbsp;实体和&amp;符号，&#96;数字实体&#96;。[图片]这里有个图[视频][表情]
      重复标点。。。。。这里！！！！
      引号测试：""这是引号""。省略号...和。。。都有。
      中文 之间 的 空格。零宽字符在这里。
      <p>段落标签</p><div>div标签</div>

输出: 第一段文字第二段开始。

      这行有四个空格缩进。还有 实体和&符号，数字实体。这里有个图重复标点……。这里！
      引号测试：“这是引号”。省略号……和……都有。
      中文之间的空格。零宽字符在这里。
      段落标签
      div标签
```

### 建议：关掉 AI 也能用了

上面这些清洗在 **AI 关闭时同样生效**（`cleanText()` 是独立的一层）。

关掉 AI 后处理是**毫秒级**，标题/作者仍来自抖音抓取，只有这两件事做不了：

- 错别字 / 网络用语规范化
- 无标题时自动概括

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
