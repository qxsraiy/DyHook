## DyHook v1.1.0 — 抖音文案提取 + 论坛发件

在 v1.0.0 基础上接入 **Flarum 论坛发件**。

### 新增：发件到论坛

点通知栏的「发件」按钮，把成品 txt 发成论坛新讨论贴：

- **标题** = 文章标题
- **正文** = `@作者by抖音` + 换行 + 文章正文
- **标签** = 本地配置的默认标签

通知栏状态在同一条通知上流转：

```
✅ 解析完成：青空_xxx.txt            [发件]
        ↓ 点击
📤 发件中…                          （按钮消失）
        ↓
✅ 发件成功：https://forum/d/9
```

- **每个任务一个独立通知 ID**，新任务发新通知，旧通知保留作发件记录
- **发件幂等锁**：同一文件 60 秒内只发一次（修掉了广播重投导致连发多贴的问题）
- **路径白名单守卫**：只允许发送输出目录下的 txt

### 论坛配置

模块界面新增「论坛发件」区：论坛地址 / 账号 / 密码 / 默认标签 slug，含**测试论坛登录**按钮。

⚠ **凭据只存在设备本地** `/sdcard/Documents/dyhooktxt/forum.txt`，
代码与仓库里**没有任何论坛地址、账号或密码**。

### 完整功能

- 抖音长文章「复制口令」→ 自动抓取**正文 + 作者（昵称/抖音号）**
- 可选 **AI 结构化**（兼容 OpenAI `/v1/chat/completions`）
- 双落盘：`源_时间戳_标题_作者.txt` + `青空_标题_作者_时间戳.txt`
- 通知栏状态流转 + 一键发件到 Flarum
- 系统分享接收：任意 App 分享文字 → 存为「个人复制」

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限，配置 AI 与论坛

### 输出位置

`/sdcard/Documents/dyhooktxt/`

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过

### 核心实现要点

- **全流程寄生在抖音进程内执行**，规避加固 ROM 的跨进程拦截
  （IntentFirewall 拦跨应用服务调用 / app freezer 跳过广播 / BAL 限制）
- 抖音模型多由 Gson/Unsafe 生成、**不走构造函数**，故 hook 构造函数 + 类内所有方法双保险
- 模块 App 自身进程**不可引用 libxposed 类**（会导致组件无法实例化），日志走独立的 `DyLog`

详细踩坑见仓库 [README](https://github.com/qxsraiy/DyHook#踩过的坑重要)。
