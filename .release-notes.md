## DyHook v1.2.0 — 正文排版 + AI 清洗优化

在 v1.1.0 基础上优化**发帖正文格式**与 **AI 清洗能力**。

### 正文格式

论坛帖子正文现在是：

```
@作者by抖音
（空 1 行）
（空 2 行）
文章正文……
```

**为什么空行要用全角空格实现**（踩坑记录）：

Flarum 的 markdown 渲染有两个坑，实测对比后确定方案：

| 方案 | 结果 |
|---|---|
| `<br>` `<br/>` | ❌ 被转义成字面文本显示出来 |
| `&nbsp;` | ❌ 被转义成 `&amp;nbsp;` |
| `\n\n\n` 连续换行 | ⚠️ HTML 里有空行，但浏览器折叠，视觉上只有 1 个段落间距 |
| **全角空格行 `U+3000`** | ✅ **渲染成真正的空段落，视觉上就是空 2 行** |

### AI 清洗能力增强

提示词要求 AI 把原文清洗成正规书面语言：

| 输入 | 输出 |
|---|---|
| `<br/>` `<br>` | 真换行 |
| `<p>` `</p>` `<div>` `<span>` `<a>` `<img>` | 删除 |
| `&nbsp;` `&amp;` `&lt;` `&gt;` `&quot;` | 还原成对应字符 |
| `""文字""` `''文字''` `` ``文字`` `` | 修正成一对中文引号 |
| 字面 `\n` `\t` `\"` `\\` | 还原成正常字符 |
| 分享口令、`复制打开抖音`、`看看【xxx的作品】` | 整行删除 |
| 连续空行、行首尾空格、硬折行 | 合并 / 去除 / 接回 |
| 网络用语、错别字、标点混用 | 轻度书面化（**不改原意、不增删信息**） |

实测效果：

```
输入: 这是一段测试文本<br/>用来验证AI清洗能力。<br/><br/>
      他说：""这个功能真不错""。<p>第二段落在这里。</p>
      还有&nbsp;实体和&amp;符号。复制打开抖音，看看【某某的作品】^^abc123

输出: 这是一段测试文本
      用来验证AI清洗能力。

      他说：“这个功能真不错”。

      第二段落在这里。还有 实体和&符号。
```

### 标签默认留空

- 默认标签改为**留空**，需在模块界面手动填写
- 留空则**不带标签发帖**（已验证可用）
- 填了 slug 但论坛里找不到该标签时，**明确报错**而不是静默失败

### 完整功能

- 抖音长文章「复制口令」→ 自动抓取**正文 + 作者（昵称/抖音号）**
- AI 结构化 + 清洗（兼容 OpenAI `/v1/chat/completions`）
- 双落盘：`源_时间戳_标题_作者.txt` + `青空_标题_作者_时间戳.txt`
- 通知栏状态流转（解析完成 → 发件中 → 发件成功，按钮自动消失）
- 一键发件到 Flarum 论坛
- 发件幂等锁 + 路径白名单守卫
- 系统分享接收：任意 App 分享文字 → 存为「个人复制」

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限，配置 AI 与论坛

### 输出位置

`/sdcard/Documents/dyhooktxt/`

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过

### 核心实现要点

- **全流程寄生在抖音进程内执行**，规避加固 ROM 的跨进程拦截
  （IntentFirewall 拦跨应用服务调用 / app freezer 跳过广播 / BAL 限制）
- 抖音模型多由 Gson/Unsafe 生成、**不走构造函数**，故 hook 构造函数 + 类内所有方法双保险
- 模块 App 自身进程**不可引用 libxposed 类**（会导致组件无法实例化），日志走独立的 `DyLog`

详细踩坑见仓库 [README](https://github.com/qxsraiy/DyHook#踩过的坑重要)。
