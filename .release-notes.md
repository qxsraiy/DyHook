## DyHook v1.4.1 — 修复「开了自动发件却不发」+ 文章引言

### 🔴 修复：开着自动发件，解析完却不发

**根因**（这是个很隐蔽的 Android 沙盒坑）：

真实流程里解析跑在**抖音进程**里，抖音进程要读 `config.txt` 才知道 `auto_send=1`。
但 Android 11+ 的 **FUSE 沙盒规则**是：

> **没有存储权限的 App，只能访问「自己创建」的文件。**

`config.txt` 是模块创建的 → 抖音读它直接 `EACCES`：

```
exists=true  canRead=false  canWrite=false
java.io.FileNotFoundException: config.txt: open failed: EACCES (Permission denied)
```

读失败后 `SharedCfg.getBool("auto_send", false)` 返回**默认值 `false`** →
不自动发件。同一个原因，`ai_enabled` 在真实流程里也会读成 `false`。

**解决**：让**抖音先创建**一个共享配置文件 `douyin_cfg.txt`：

```
/storage/emulated/0/Documents/dyhooktxt/douyin_cfg.txt
```

之后：

- 抖音能读写**自己创建**的文件（永远可以，不需要任何权限）
- 模块有 `MANAGE_EXTERNAL_STORAGE`，也能读写它

模块启动时会把配置**同时写两份**，抖音优先读共享那份。修复后实测：

```
共享配置文件可用=true  auto_send=true  ai_enabled=true    ← 抖音进程正确读到
```

> 附注：`/sdcard/Android/data/<pkg>/` 对其它 App 是**硬隔离**的，
> 连 `MANAGE_EXTERNAL_STORAGE` 都进不去，所以不能用那个目录。

**升级后请做一次**：打开一次模块界面（会自动同步配置），然后**强制停止抖音再打开**。

### ✨ 新增：文章引言 / 备注

抖音长文章的摘要（引言）现在会被提取出来，放在**作者下面空行的下面**，作为正文首行：

```
@作者by抖音

（空 1 行）
引言 / 摘要内容……

正文第一段……
```

- 从 `articleContent` JSON 里找，兼容多种 key：
  `long_article_abstract` / `article_abstract` / `abstract` / `summary` /
  `digest` / `introduction` / `lead` / `preface` / `desc` / `description`
- 如果引言本来就包含在正文开头，**不会重复添加**
- 日志里会打印 `[文章] JSON keys=… | 引言=N字`，方便确认抖音用的字段名

### 之前版本已包含

- 待发件列表点击确认删除（成品 + 源文件一起删）
- 代码级强制清洗（`<br/>`、HTML 实体、代码围栏、行首缩进）
- 修复论坛「代码框」问题
- 自动发件开关 / 底栏两页 / 作者名五级兜底
- 正文格式、通知状态流转、10 次自动重试
- 修复 APK 安装失败（`resources.arsc` 对齐）

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. 打开模块 App 一次（同步配置）
4. **强制停止抖音再打开**（必须）
5. 模块界面确认存储权限

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
