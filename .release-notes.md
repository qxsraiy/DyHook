## 抖音文案提取 DyHook v1.0.0

抖音长文章「复制口令」后自动抓取正文并交给 AI 结构化，落盘为 txt 并弹出通知栏。

### 功能

- 文章页分享 → 复制口令，自动抓取 **正文 + 作者（昵称/抖音号）**
- 可选 **AI 结构化**（兼容 OpenAI `/v1/chat/completions`，支持 DeepSeek / NewAPI 等）
- 双落盘：`源_时间戳_标题_作者.txt`（原文）+ `青空_标题_作者_时间戳.txt`（AI 成品）
- 通知栏进度 + 完成通知（带「发件」按钮）
- 系统分享接收：任意 App 分享文字过来 → 存为「个人复制」
- 模块界面可配置 AI 地址 / Key / 模型，含「测试 AI 连接」按钮

### 安装

1. 安装 APK
2. LSPosed 里启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须，否则新逻辑不生效）
4. 打开模块 App 授予存储权限，按需配置 AI

### 输出位置

`/sdcard/Documents/dyhooktxt/`

成品格式：

```
标题：xxx
作者：昵称（抖音号）by抖音
时间：2026-09-24 11:55:52
来源：抖音文章
==========

正文……
```

### 环境要求

- LSPosed（支持 LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过

### 当前进度

| 项目 | 状态 |
|---|---|
| 抖音文字下载（正文 + 作者/抖音号） | ✅ 完成并真机验证 |
| AI 修正 | ✅ 完成并真机验证 |
| 源文件 + 成品双落盘、通知栏 | ✅ 完成 |
| 系统分享接收 | ✅ 完成 |
| 「发件」按钮对接论坛 | ⏳ 待做（`SendActionReceiver` 已留入口） |
| 视频转文字 | ❌ 已按需求移除 |

### 核心实现要点

- **全流程寄生在抖音进程内执行**（AI + 落盘 + 通知），规避加固 ROM 的跨进程拦截：
  IntentFirewall 拦跨应用服务调用 / app freezer 跳过广播 / BAL 限制后台启动 Activity
- 抖音模型多由 Gson/Unsafe 反序列化生成、**不走构造函数**，故 hook **构造函数 + 类内所有方法** 双保险捕获实例
- 模块 App 自身进程**不可引用 libxposed 类**（会导致 Activity/Service 无法实例化），
  日志走独立的 `DyLog`
- 文章路径下 hook 抓到的作者（含抖音号）是**权威来源**，AI 不允许覆盖

详细踩坑记录见仓库 [README](https://github.com/qxsraiy/DyHook#踩过的坑重要)。
