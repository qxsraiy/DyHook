## DyHook v1.2.2 — 发件可靠性 + 作者修复

### 1. 修复作者名串到上一条视频/文章

`Models.readAuthor()` 原来有个**危险兜底**：当前文章找不到作者时，会从历史缓存的
`Aweme` 列表里随便挑一个填上 —— 于是作者就串成了上一条看过的视频。

现在只认两种来源，**宁缺毋滥**：

1. `ArticleDetailInfo.aweme`（它就是当前文章的详情对象）
2. `awemeList` 里 `articleInfo.articleId == 当前文章 id` 的那个

都找不到就留空，绝不乱填。

### 2. 发件自动重试

原来失败一次就结束，现在：

- **最多重试 10 次，每次间隔 3 秒**，直到成功
- 通知上实时显示 `第 N/10 次尝试…`
- 登录失败（`connection closed`）、发帖失败都会自动重试

### 3. 成功 / 失败的不同处理

| 结果 | 行为 |
|---|---|
| **成功** | 删除本地的**源文件**和**成品文件**，通知变「✅ 发件成功」 |
| **失败（重试 10 次仍失败）** | **保留文件**，通知上出现「**重试**」按钮，可手动再发 |

### 4. 修复标题过短导致发帖失败

原来遇到短标题会直接 422：

```
HTTP 422: title 不得少于 3 个字符。
```

现在 `normalizeTitle()` 会兜底：去换行、压缩空格、不足 3 字补足、超 80 字截断。

### 5. 通知栏只留一条

点「发件」后，模块会广播通知抖音进程**撤掉它自己发的那条「解析完成」通知**，
通知栏上只保留模块的「发件中 → 发件成功」。

（实现：`HookEntry` 在抖音进程注册接收器，`SendActionReceiver` 发广播触发撤销）

### 6. 正文格式微调

作者名下面从空 2 行改为**空 1 行**：

```
@作者by抖音
（空 1 行）
文章正文……
```

### 顺带修的问题

**卸载重装后权限丢失**：测试安装时卸载过 App，`MANAGE_EXTERNAL_STORAGE` 授权被重置，
导致模块读不到 `config.txt`（表现为「AI 未开启」）。重装后请确认模块界面里
存储权限是已授权状态。

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限，配置 AI 与论坛

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
