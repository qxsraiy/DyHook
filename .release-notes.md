## DyHook v1.4.5 — 发件串行队列 + token 缓存

### 1. 串行队列：一次只发一篇

之前每个发件入口都是**各开一个线程并行发**，点 10 个就是 10 条线程同时登录同时发，
容易撞论坛的 **429 限流**。

现在全部走**单线程队列**：

```
列表点「发件」 ┐
通知栏「发件」 ├─→ 单线程队列 ─→ 一篇发完 → 下一篇 → …
自动发件      ┘
```

- 一个发完再发下一个
- **失败重试也排着来**（10 次 × 3 秒），不会互相叠加
- 通知里会显示「（队列里还有 N 篇在等）」
- 队列线程是 daemon，进程退出不留残留

### 2. token 缓存：不再每篇都登录

之前 `createDiscussion()` 每次进来都先 `login()`，**发 10 篇就登录 10 次**。

现在加了 token 缓存（Flarum 的 token 是长效的）：

| | 之前 | 现在 |
|---|---|---|
| 发 10 篇 | **10 次登录** | **1 次登录** |
| token 失效 | 不处理 | **遇 401/403 自动清缓存重登一次再发** |
| 改论坛配置 | — | 自动清 token 缓存（旧 token 作废） |

实测日志会变成：

```
[论坛] 登录成功 userId=2
[论坛] 复用已缓存 token
[论坛] 复用已缓存 token
...
```

### 已包含（v1.4.4）

- 待发件列表**固定显示洗稿后的成品**（`青空_*`），不跟 AI 开关切换
- 修复某些文章**正文重复两遍**（删掉把 `long_article_abstract` 当摘要拼接的逻辑）

### 已包含（v1.4.3）

本地洗稿能力（纯正则，不依赖 AI）：HTML 标签/实体、代码围栏与行首缩进、
分享口令、12 条引流话术、零宽字符、占位符、重复标点与引号规范化。

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
