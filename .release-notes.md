## DyHook v1.4.0 — 列表删除确认 + 清洗加固 + 界面修复

### 1. 待发件列表支持「点击删除」

点击列表**任意一行**（不是右侧的发件按钮）会弹出确认框：

```
确认删除吗？
<文章标题>

同时会删除源文件：
源_20260924_221048_xxx.txt

删除后不可恢复。
        [取消]  [删除]
```

确认后**成品和源文件一起删**。源文件是按「标题 + 时间戳」自动配对的。

### 2. HTML / 代码框清洗加固（代码级，不依赖 AI 是否听话）

之前只靠提示词，`<br/>` 还是会漏。现在加了**代码级强制清洗** `AiClient.cleanText()`，
在 AI 输出、AI 关闭时的原文、发帖前**三个位置都会过一遍**：

| 输入 | 输出 |
|---|---|
| `<br>` `<br/>` `<br />` | 真换行 |
| `<p>` `</p>` `<div>` `<span>` `<a>` `<img>` 等 | 删除 |
| `&nbsp;` `&amp;` `&lt;` `&quot;` | 还原成对应字符 |
| `&#96;` `&#x60;` 数字实体 | 解码 |
| ` ``` ` 代码围栏 | 删除 |
| `` ` `` 反引号 | 删除 |
| **行首 4+ 空格 / Tab / 全角空格** | 删除（**这是 Flarum 代码框的触发条件**） |
| 连续空行 | 收敛成最多一个空行 |

实测（AI 关闭，纯代码清洗）：

```
输入: 甲段文字<br/>乙段开始。<br/><br/>    这行有四个空格缩进```codefence```
      还有&#96;数字实体&#96;和&nbsp;空格&amp;符号。<p>段落标签</p><div>div标签</div>

输出: 甲段文字
      乙段开始。

      这行有四个空格缩进codefence还有数字实体和 空格&符号。段落标签

      div标签
```

发到论坛后**不再出现代码框**（已实测帖子 HTML）。

### 3. 提示词强化

明确禁止 AI 输出任何 HTML 标签和 markdown 语法：

- 不许出现 `<br/>` 之类标签，换行直接用换行符
- 不许用 ` ``` ` 围栏、反引号
- **每行开头不许有 4 个以上空格 / Tab / 缩进**（会被渲染成代码框）
- 不许用 `#` 标题、`>` 引用、`-` 列表符号

### 4. 修复模块界面标题遮挡

- `ScrollView` 补上 `MATCH_PARENT` 布局参数（之前被压扁导致内容裁切）
- 增加**状态栏 / 导航栏内边距处理**，标题不再被系统栏压住
- 开启 `setFitsSystemWindows`

### 5. 之前版本已包含

- 自动发件开关（开启后只用提示条告知成功/失败）
- 底栏两页导航（设置 / 待发件）
- 作者名五级兜底获取
- 正文格式：`@作者by抖音` + 空 1 行 + 正文
- 发件 10 次自动重试 + 成功后删文件
- 通知栏状态流转
- 修复 APK 安装失败（`resources.arsc` 对齐）

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限

> ⚠ 卸载重装会重置 `MANAGE_EXTERNAL_STORAGE`，重装后请在模块界面确认存储权限。

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
