## DyHook v1.4.2 — 修复通知卡住 + 自动发件失败的真正原因

上一版只修了 `config.txt`，这一版把**同一个 FUSE 沙盒坑的其余两处**一并修掉。

### 🔴 根因回顾：FUSE 沙盒

Android 11+ 的规则：

> **没有存储权限的 App，只能访问「自己创建」的文件。**

解析跑在**抖音进程**里，而下面这些文件都是**模块创建**的，抖音读/写全部 `EACCES`：

| 文件 | 后果 |
|---|---|
| `config.txt` | `auto_send` / `ai_enabled` 读成默认 **false** → **不自动发件**（v1.4.1 已修） |
| `forum.txt` | 论坛凭据读不到 → `cfg.ready()=false` → **「未配置论坛」→ 发件失败**（本版修） |
| `dyhook.log` | 抖音进程的日志**写不进去** → 出问题完全看不到原因（本版修） |

### 修复：全部改为「抖音先创建，模块写内容」

抖音启动时创建这三个文件，之后双方都能访问：

```
/sdcard/Documents/dyhooktxt/douyin_cfg.txt     配置（auto_send / ai_enabled / AI 参数）
/sdcard/Documents/dyhooktxt/douyin_forum.txt   论坛凭据
/sdcard/Documents/dyhooktxt/douyin.log         抖音进程的日志
```

模块启动时会把配置**同时写两份**，抖音优先读共享那份。修复后实测：

```
共享配置文件可用=true  auto_send=true  ai_enabled=true
```

### 🔴 修复：通知永远卡在「AI 分析中」

那条卡住的通知是**抖音包**发的（`pkg=com.ss.android.ugc.aweme id=1001`，`ONGOING_EVENT`）。

原因：寄生路径（抖音进程内直接跑）**没有前台服务**，没人调 `stopForeground()`，
所以进度通知永远挂着。

修复：`AiProcessService.run()` 结束时显式 `Notifier.cancelProgress(ctx)`，
两条路径（前台服务 / 寄生）都会清理。

### 新增诊断日志

- `douyin.log` 现在能记录**抖音进程**的全部日志（之前是空的）
- 发件时打印：标题 / 正文首行 / 正文长度 / 论坛地址 / 标签 / **第 N 次尝试**
- 启动时打印：共享配置文件可用性 + 读到的 `auto_send` / `ai_enabled`

### 升级后请做

1. **强制停止抖音再打开**（让抖音创建三个共享文件）
2. **打开一次模块界面**（把配置写进共享文件）
3. 再强制停止抖音打开一次

之后 `auto_send` 就会真正生效。

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
