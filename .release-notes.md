## DyHook v1.2.3 — 作者名获取（五级兜底）

### 目标：作者名必须拿到

上一版为了修「作者串到上一条视频」，把兜底路径**砍过头了** —— 连之前能正常拿到作者的
路径也一起砍掉。这一版重新梳理成**五级兜底**，既拿到作者，又不会串号。

### 五级兜底（按优先级）

| 级别 | 来源 | 说明 |
|---|---|---|
| 1 | `ArticleDetailInfo.aweme.author` | 当前文章的详情对象，最准 |
| 2 | `ArticleDetailResponse.d.author` | API 响应里的 Aweme（新增捕获源） |
| 3 | `awemeList` 中 `articleInfo.articleId` **严格匹配**的 Aweme | 精确匹配 |
| 4 | `awemeList` 中**带 `articleInfo` 的 Aweme**（只认文章类） | **这是之前能拿到作者的关键路径** |
| 5 | `ArticleInfoStruct` 的 `feData` / `articleExtra` JSON | Lynx 前端数据，递归扫 `nickname` / `unique_id` 等 key |

**关键点**：只排除「任意 Aweme（含视频）」这一层——那才是串号的元凶；
「文章类 Aweme」这一层必须保留。

### 作者字段读取加强

`fillAuthorFromAweme()` 现在会试更多字段名和嵌套结构：

- 昵称：`nickname` / `nickName` / `nick_name` / `displayName` / `name` / `uniqueId`
- 抖音号：`uniqueId` / `unique_id` / `shortId` / `short_id` / `uid` / `secUid`
- 作者对象：`author` / `authorUser` / `user`

### 新增诊断日志

每次解析都会输出一行诊断，一眼看出作者藏在哪个源里：

```
[作者诊断] articleId=xxx | ArticleInfoStruct=有 | DetailInfo=有 | Response=有
          | Aweme缓存=12(匹配 1) | feData=2400 | articleExtra=0
[作者] 来源=Aweme 缓存(文章类，含 articleInfo)
```

### 顺带修复

- **卸载重装后权限丢失**：`MANAGE_EXTERNAL_STORAGE` 会被重置，导致模块读不到
  `config.txt`（表现为「AI 未开启」）。重装后请在模块界面确认存储权限。

### 安装

1. 安装 APK
2. LSPosed 启用模块，作用域勾选 **抖音**
3. **强制停止抖音再打开**（必须）
4. 打开模块 App 授予存储权限

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
