## DyHook v1.4.7 — 修正短标题补全逻辑

### 上一版做错了

v1.4.6 在标题不足时拼成了 `人by抖音青岚`，导致：

- 文件名变成 `青空_人by抖音青岚_青岚（qinglan123）by抖音_…`（`青岚` 和 `by抖音` 各出现两遍，很怪）
- 目的本来是**避免论坛报 422「标题不得少于 3 个字符」**，结果反而把标题搞乱了

### 现在改成：用作者昵称把标题补到 3 字以上

```
原标题「人」 + 作者「青岚（qinglan123）」 → 标题「人青岚」
```

- 只拼**纯昵称**，不带 `by抖音`、不带抖音号
- 只在标题**不足 3 字**时触发（不论 AI 开关，因为这是论坛的硬校验）
- 昵称已在标题末尾时不重复拼

实测：

```
[服务] 标题不足 3 字，已用作者补全: 「人」→「人青岚」
青空_人青岚_青岚（qinglan123）by抖音_20260925_141656.txt
```

标题 `人青岚` 是 3 个字，论坛校验能过。

> 兜底：万一补完还不足 3 字（作者名也为空），
> `ForumClient.normalizeTitle()` 仍会再补足，保证发帖不会因标题报错。

### 完整流程（v1.4.6 起）

```
┌─ 抖音分享
│   ├─ AI 关：原文 → 本地洗稿 → 保存/发件
│   └─ AI 开：原文 → AI 洗稿 → 本地洗稿 → 保存/发件
│
└─ 第三方分享
    └─ 无论 AI 开关：原文 → 强制 AI 重写 → 本地洗稿 → 保存/发件
        （专用提示词：内容混乱需重写通顺、标题至少 3 字）
```

### 环境

- LSPosed（LibXposed API 102）
- Android 8.0+（compileSdk 37 / AGP 9.3.x / Gradle 9.5.1）
- 抖音 38.x 实测通过
