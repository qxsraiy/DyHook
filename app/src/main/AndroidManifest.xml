<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:label="抖音文案提取"
        android:icon="@android:drawable/sym_def_app_icon"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 强制竖屏 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 系统分享接收：任意 App 分享文字过来 -->
        <activity
            android:name=".ShareReceiverActivity"
            android:label="抖音文案提取"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".SettingsAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity-alias>

        <!-- AI 处理前台服务（模块自己的进程，保活处理过程）
             exported=true: 允许抖音进程里的 hook 直接启动它，
             避免 App 被系统冻结时广播被跳过。 -->
        <service
            android:name=".AiProcessService"
            android:exported="true"
            android:foregroundServiceType="dataSync" />

        <!-- 统一处理入口：抖音 hook / 分享都发到这里 -->
        <receiver
            android:name=".ProcessReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.dyhook.txt.PROCESS" />
            </intent-filter>
        </receiver>

        <!-- 「发件」按钮 -->
        <receiver
            android:name=".SendActionReceiver"
            android:exported="false" />

        <provider
            android:name="io.github.libxposed.service.XposedProvider"
            android:authorities="com.dyhook.txt.XposedService"
            android:exported="true" />
    </application>
</manifest>
